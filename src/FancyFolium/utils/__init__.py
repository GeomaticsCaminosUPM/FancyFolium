"""
FancyFolium.utils
==================
Internal helper modules: colour scales (``color``), bounds/CRS helpers
(``geo``), raster I/O (``raster``), and the tile-provider registry
(``tiles``). Re-exported from the top-level ``FancyFolium`` package —
import from there rather than this subpackage directly.
"""
